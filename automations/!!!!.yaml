  - alias: Godnatt vid låg volym
    mode: queued

    trigger:
      platform: numeric_state
      entity_id: sensor.yamaha_volume_level
      below: '25' # < 75

    action:
      - service: script.goodnight_all
