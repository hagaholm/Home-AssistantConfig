    ##############################################################
    ### Extra varning för hög temp i frysen                    ###
    ### T: sensor.frys_temperature > - 12                      ###
    ### A: notify.pushbullet "LARM! Varning hög temp i frysen" ###
    ##############################################################
  - alias: Larm! Varning hög temp i frys
    initial_state: 'on'
    trigger:
      platform: numeric_state
      entity_id: sensor.frys_temperature
      above: '-12'
    action:
      - service: script.notify_engine
        data_template:
          value1: 'LARM! Varning hög temp i frysen: {{ states.sensor.frys_temperature.state }} {{ states.sensor.frys_temperature.unit_of_measurement }}'
      - delay: '00:00:10'
      - service: script.notify_engine
        data_template:
          value1: 'LARM! Varning hög temp i frysen: {{ states.sensor.frys_temperature.state }} {{ states.sensor.frys_temperature.unit_of_measurement }}'

    ######################################################
    ### Varnar för hög temp i frysen                   ###
    ### T: sensor.frys_temperature > - 16              ###
    ### A: notify.pushbullet "LARM! Hög temp i frysen" ###
    ######################################################
  - alias: Larm! Hög temp i frys
    initial_state: 'on'
    trigger:
      platform: numeric_state
      entity_id: sensor.frys_temperature
      above: '-16'
    action:
      - service: script.notify_engine
        data_template:
          value1: 'LARM! Hög temp i frysen: {{ states.sensor.frys_temperature.state }} {{ states.sensor.frys_temperature.unit_of_measurement }}'

    ####################################################
    ### Varnar för låg temp i Frys                   ###
    ### T: sensor.frys_temperature < - 25            ###
    ### A: notify.pushbullet "LARM! Låg temp i Frys" ###
    ####################################################
  - alias: Larm! Låg temp i frys
    initial_state: 'on'
    trigger:
      platform: numeric_state
      entity_id: sensor.frys_temperature
      below: '-25'
    action:
      - service: script.notify_engine
        data_template:
          value1: 'LARM! Låg temp i frysen: {{ states.sensor.frys_temperature.state }} {{ states.sensor.frys_temperature.unit_of_measurement }}'
