    #######################################################
    ### Tänder morgonbelysning                          ###
    ### T: input_select.light_inside == Morgonbelysning ###
    ### A: script.morninglight_on                       ###
    #######################################################
  - alias: Tänd morgonbelysning
    mode: single

    trigger:
      - platform: state
        entity_id: input_select.light_inside
        to: Morgonbelysning

    action:
      - service: script.morning_lights_on

    ####################################################
    ### Tänder dagbelysning                          ###
    ### T: input_select.light_inside == Dagbelysning ###
    ### A: script.daylights_on                       ###
    ####################################################
  - alias: Tänd dagbelysning
    mode: single

    trigger:
      - platform: state
        entity_id: input_select.light_inside
        to: Dagbelysning

    action:
      - service: script.daylights_on

    #####################################################
    ### Tänder nattbelysning                          ###
    ### T: input_select.light_inside == Nattbelysning ###
    ### A: script.night_light                         ###
    #####################################################
  - alias: Tänd nattbelysning
    mode: single

    trigger:
      - platform: state
        entity_id: input_select.light_inside
        to: Nattbelysning

    action:
      - service: script.night_light

    ######################################################
    ### Tänder bortabelysning                          ###
    ### T: input_select.light_inside == Bortabelysning ###
    ### A: script.away_lights_on                       ###
    ######################################################
  - alias: Tänd bortabelysning
    mode: single

    trigger:
      - platform: state
        entity_id: input_select.light_inside
        to: Bortabelysning

    action:
      - service: script.away_lights_on

    ###################################################################
    ### Tänder fönterbelysning                                      ###
    ### För att julbelysning ska vara tänd även om det är ljust ute ###
    ### T: input_select.light_inside == Fönsterbelysning            ###
    ### A: script.christmas_lights_on                               ###
    ###################################################################
  - alias: Tänd fönsterbelysning
    mode: single

    trigger:
      - platform: state
        entity_id: input_select.light_inside
        to: Fönsterbelysning

    action:
      - service: script.window_lights_on

    ##############################################
    ### Släcker innebelysning                  ###
    ### T: input_select.light_inside == Släckt ###
    ### A: script.daylights_off                ###
    ##############################################
  - alias: Släcker innebelysning
    mode: single

    trigger:
      - platform: state
        entity_id: input_select.light_inside
        to: Släckt

    action:
      - service: script.daylights_off


#       - Släckt
#       - Morgonbelysning
#     - Förmiddagsbelysning
#       - Dagbelysning
#     - Kvällsbelysning
#       - Nattbelysning
#       - Bortabelysning
#     - Arbetsbelysning
