    #############################################################################
    ### Tänder nattbelysning vid rörelse i köket vid hemma av och dagsljus på ###
    ### T: binary_sensor.kok_narvaro = on                                     ###
    ### C: input_select.home_status == 'Hemma av'                             ###
    ###    input_boolean.daylight_on == on                                    ###
    ### A: input_select.light_inside = Nattbelysning                          ###
    #############################################################################
  - alias: Nattbelysning on
    id: '7188810450138' 

    trigger:
      platform: state
      entity_id: binary_sensor.kok_narvaro
      to: 'on'

    condition:
      condition: and
      conditions:
        - condition: state
          entity_id: input_select.home_status
          state: 'Hemma av'
        - condition: state
          entity_id: input_boolean.daylight_on
          state: 'on'

    action:
      - service: input_select.select_option
        data:
          entity_id: input_select.light_inside
          option: Nattbelysning

    ####################################################
    ### Släcker nattbelysning efter sista aktivering ###
    ### T: binary_sensor.kok_narvaro == off          ###
    ### C: input_select.home_status == 'Hemma av'    ###
    ### A: input_select.light_inside = Släckt        ###
    ####################################################
  - alias: Nattbelysning off
    id: '2393518316319' 

    trigger:
      platform: state
      entity_id: binary_sensor.kok_narvaro
      to: 'off'

    condition:
      condition: and
      conditions:
        - condition: state
          entity_id: input_select.home_status
          state: 'Hemma av'

    action:
      - service: input_select.select_option
        data:
          entity_id: input_select.light_inside
          option: Släckt
