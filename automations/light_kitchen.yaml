    ########################################################
    ### Tänder lampor 100% vid rörelse i köket           ###
    ### T: input_boolean.rorelsedetektor_kok_aktiv == on ###
    ### C: input_boolean.kitchen_light_on == on          ###
    ###    input_select.home_status == 'Hemma på'        ###
    ### A: input_select.light_kitchen = 100%             ###
    ########################################################
  - alias: Kök 100% (hemma på)
    id: '9716374235056' 
    mode: queued

    trigger:
      - platform: state
        entity_id: input_boolean.rorelsedetektor_kok_aktiv
        to: 'on'

    condition:
      condition: and
      conditions:
        - condition: state
          entity_id: input_boolean.kitchen_light_on
          state: 'on'
        - condition: state
          entity_id: input_select.home_status
          state: 'Hemma på'

    action:
      - service: input_select.select_option
        data:
          entity_id: input_select.light_kitchen
          option: 100%

    ########################################################
    ### Tänder lampor 100% vid rörelse i köket           ###
    ### T: input_boolean.rorelsedetektor_kok_aktiv == on ###
    ### C: input_boolean.kitchen_light_on == on          ###
    ###    input_select.home_status == 'Borta'           ###
    ### A: input_select.light_kitchen = 100%             ###
    ########################################################
  - alias: Kök 100% (borta)
    id: '1428426341574' 
    mode: queued

    trigger:
      - platform: state
        entity_id: input_boolean.rorelsedetektor_kok_aktiv
        to: 'on'

    condition:
      condition: and
      conditions:
        - condition: state
          entity_id: input_boolean.kitchen_light_on
          state: 'on'
        - condition: state
          entity_id: input_select.home_status
          state: 'Borta'

    action:
      - service: input_select.select_option
        data:
          entity_id: input_select.light_kitchen
          option: 100%

    ##################################################################
    ### Tänder lampor 100% när det blir mörkt och någon är i köket ###
    ### T: input_boolean.kitchen_light_on == on                    ###
    ### C: input_boolean.rorelsedetektor_kok_aktiv == on           ###
    ###    input_select.home_status == 'Hemma på'                  ###
    ### A: input_select.light_kitchen = 100%                       ###
    ##################################################################
  - alias: Kök 100% (mörkt)
    id: '1795919757331' 
    mode: queued

    trigger:
      - platform: state
        entity_id: input_boolean.kitchen_light_on
        to: 'on'

    condition:
      condition: and
      conditions:
        - condition: state
          entity_id: input_boolean.rorelsedetektor_kok_aktiv
          state: 'on'
        - condition: state
          entity_id: input_select.home_status
          state: 'Hemma på'

    action:
      - service: input_select.select_option
        data:
          entity_id: input_select.light_kitchen
          option: 100%

    ########################################################
    ### Tänder lampor 25% vid rörelse i köket            ###
    ### T: input_boolean.rorelsedetektor_kok_aktiv == on ###
    ### C: input_boolean.kitchen_light_on == on          ###
    ###    input_select.home_status == 'Hemma av'        ###
    ### A: input_select.light_kitchen = 25%              ###
    ########################################################
  - alias: Kök 25% (hemma av)
    id: '1974703878867' 
    mode: queued

    trigger:
      - platform: state
        entity_id: input_boolean.rorelsedetektor_kok_aktiv
        to: 'on'

    condition:
      condition: and
      conditions:
        - condition: state
          entity_id: input_boolean.kitchen_light_on
          state: 'on'
        - condition: state
          entity_id: input_select.home_status
          state: 'Hemma av'

    action:
      - service: input_select.select_option
        data:
          entity_id: input_select.light_kitchen
          option: 25%

    #########################################################
    ### Släcker lampor när ingen varit i köket på 9 min   ###
    ### T: input_boolean.rorelsedetektor_kok_aktiv == off ###
    ### A: input_select.light_kitchen = Släckt            ###
    #########################################################
  - alias: Kök off (ingen rörelse)
    id: '0869914950923' 
    mode: single

    trigger:
      platform: state
      entity_id: input_boolean.rorelsedetektor_kok_aktiv
      to: 'off'

    action:
      - service: input_select.select_option
        data:
          entity_id: input_select.light_kitchen
          option: Släckt

    ########################################################
    ### Släcker lampor när sista åker hemifrån           ###
    ### T: input_select.home_status to: 'Borta'          ###
    ### A: input_boolean.rorelsedetektor_kok_aktiv = off ###
    ########################################################
  - alias: Kök off (borta)
    id: '4859375043622' 
    mode: single

    trigger:
      - platform: state
        entity_id: input_select.home_status
        to: 'Borta'

    action:
      - service: homeassistant.turn_off
        entity_id: input_boolean.rorelsedetektor_kok_aktiv

    ##############################################################
    ### Släcker lampor när lux kommer över 3000                ###
    ### T: input_boolean.kitchen_light_on == off               ###
    ### C: group.track_for_home_devices == 'home'              ###
    ### A: input_boolean.rorelsedetektor_kok_aktiv = off       ###
    ##############################################################
  - alias: Kök off (ljust)
    id: '5032661568007' 
    mode: single

    trigger:
      - platform: state
        entity_id: input_boolean.kitchen_light_on
        to: "off"

    condition:
      condition: state
      entity_id: group.track_for_home_devices
      state: 'home'

    action:
      - service: homeassistant.turn_off
        entity_id: input_boolean.rorelsedetektor_kok_aktiv

    #########################################################################
    ### Sätter rörelsedetektor till off, för att kunna detektera nästa on ###
    ### Löser problemet med att lyset i vissa fall inte tänds i köket     ###
    ### T: light.kok_underskap_bank == off                                ###
    ### C: input_boolean.rorelsedetektor_kok_aktiv == on                  ###
    ### A: input_boolean.rorelsedetektor_kok_aktiv = off                  ###
    #########################################################################
  - alias: Kök Rörelsedetektor reset
    id: '0766102777583' 
    mode: single

    trigger:
      - platform: state
        entity_id:
          - light.kok_underskap_bank
        to: 'off'

    condition:
      condition: state
      entity_id: input_boolean.rorelsedetektor_kok_aktiv
      state: 'on'

    action:
      - service: homeassistant.turn_off
        entity_id: input_boolean.rorelsedetektor_kok_aktiv
