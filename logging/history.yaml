#-------------------------------------------------------
# Enables support for tracking state changes over time
# https://home-assistant.io/components/history/
#-------------------------------------------------------
# Note: The 'include' and 'exclude' options are deprecated
# Modern Home Assistant automatically tracks all entities
# No specific configuration needed - history component is enabled by default

