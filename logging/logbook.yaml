#---------------------------------------------------
# View all events in a logbook
#---------------------------------------------------
  include:
    domains:
      - automation
      - light
      - media_player
      - sensor
#      - switch
  exclude:
    domains:
      - switch
    entities:
#      - switch.rorelsevakt_kok
      - sensor.yr_symbol
      - sensor.since_last_boot 
      - sensor.since_last_boot_templated
      - sensor.ha_uptime
      - automation.set_timers_when_there_is_movement_in_kitchen
