automation:
  - alias: "Godnatt"
    id: '6728860851847' 
    mode: queued

    trigger:
      platform: state
      entity_id: switch.ha_godnatt
      to: 'on'

    action:
      - service: script.goodnight_all
      - delay: '00:00:05'
      - service: homeassistant.turn_off
        entity_id: switch.ha_godnatt