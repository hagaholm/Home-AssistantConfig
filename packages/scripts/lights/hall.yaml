# Scripts for hall lighting - split out of packages/lights/hall.yaml (Fas 2)
# Full documentation: packages/lights/hall.yaml

script:

  hall_set_correct_mode:
    alias: Återställ hallbelysning (sätt korrekt läge)
    mode: single
    sequence:
      - variables:
          home_status: "{{ states('input_select.home_status') }}"
          hall_state: "{{ states('light.hall_tak') }}"
          bri: "{{ state_attr('light.hall_tak', 'brightness') }}"
          bri_pct: >-
            {% if bri is number %}
              {{ ((bri | float(0)) * 100 / 255) | round(0) | int }}
            {% else %}
              -1
            {% endif %}
          current_option: "{{ states('input_select.light_hall') | trim }}"
          valid_options:
            - Släckt
            - 1%
            - 10%
            - 50%
            - 100%
          desired_option: >-
            {% if home_status != 'Hemma' %}
              Släckt
            {% elif hall_state != 'on' %}
              Släckt
            {% elif bri_pct < 0 %}
              100%
            {% elif bri_pct <= 5 %}
              1%
            {% elif bri_pct <= 25 %}
              10%
            {% elif bri_pct <= 75 %}
              50%
            {% else %}
              100%
            {% endif %}

      - choose:
          - conditions:
              - condition: template
                value_template: "{{ (desired_option | trim) in valid_options and (desired_option | trim) != current_option }}"
            sequence:
              - service: input_select.select_option
                data:
                  entity_id: input_select.light_hall
                  option: "{{ desired_option | trim }}"

        default: []

#######################################################
#                                                     #
#              HALL TAKLAMPOR SCRIPTS                 #
#                                                     #
#######################################################
#
# BESKRIVNING:
# Förinställda nivåer för halltak (off/1/10/50/100%).
#
# SÄKERHET:
# - Mode: single (för att undvika samtidiga dim-ändringar)
# - Tillgänglighetskontroll för light.hall_tak
#######################################################

  ##########################
  ### Hall taklampor OFF ###
  ##########################
  hall_taklampor_off:
    alias: Hall taklampor off
    mode: single
    sequence:
      - condition: template
        value_template: "{{ states('light.hall_tak') != 'unavailable' }}"
      - service: light.turn_off
        target:
          entity_id: light.hall_tak
        data:
          transition: 10

  hall_taklampor_1_procent:
    alias: Hall taklampor 1 procent
    mode: single
    sequence:
      - condition: template
        value_template: "{{ states('light.hall_tak') != 'unavailable' }}"
      - service: light.turn_on
        target:
          entity_id: light.hall_tak
        data:
          brightness_pct: 1
          transition: 5

  hall_taklampor_10_procent:
    alias: Hall taklampor 10 procent
    mode: single
    sequence:
      - condition: template
        value_template: "{{ states('light.hall_tak') != 'unavailable' }}"
      - service: light.turn_on
        target:
          entity_id: light.hall_tak
        data:
          brightness_pct: 10
          transition: 5

  hall_taklampor_50_procent:
    alias: Hall taklampor 50 procent
    mode: single
    sequence:
      - condition: template
        value_template: "{{ states('light.hall_tak') != 'unavailable' }}"
      - service: light.turn_on
        target:
          entity_id: light.hall_tak
        data:
          brightness_pct: 50
          transition: 5

  hall_taklampor_100_procent:
    alias: Hall taklampor 100 procent
    mode: single
    sequence:
      - condition: template
        value_template: "{{ states('light.hall_tak') != 'unavailable' }}"
      - service: light.turn_on
        target:
          entity_id: light.hall_tak
        data:
          brightness_pct: 100
          transition: 5
