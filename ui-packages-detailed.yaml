title: Home Assistant
views:
  #################################################################
  # FRIGATE - Detailed package breakdown
  #################################################################
  - path: frigate
    title: Frigate NVR
    icon: mdi:cctv
    theme: default
    badges: []
    cards:
      - type: heading
        heading: 📹 Frigate Package Structure

      - type: markdown
        content: |
          **Package Overview:**
          Complete NVR solution with AI-powered detection for security monitoring.

      - type: markdown
        content: |
          **4 Files in packages/frigate/:**
          📹 frigate_animal_detected.yaml
          📷 frigate_media.yaml
          👤 frigate_person_detected.yaml
          🔧 frigate_sensors.yaml

      - type: heading
        heading: 📹 Frigate Sensors (frigate_sensors.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          🎥 `binary_sensor.frigate_recording` - Recording Active status
          💻 `sensor.frigate_stats_cpu` - CPU Load percentage
          🛠️ `sensor.frigate_stats_memory` - Memory usage

      - type: heading
        heading: � Animal Detection (frigate_animal_detected.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          📹 `binary_sensor.frigate_animal_motion` - Motion detection status
          📊 `counter.frigate_animal_count` - Total detections count

      - type: heading
        heading: 👤 Person Detection (frigate_person_detected.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          👁️ `binary_sensor.frigate_person_motion` - Person detection status
          📊 `counter.frigate_person_count` - Total person detections

      - type: heading
        heading: 🎬 Media Management (frigate_media.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          💾 `sensor.frigate_storage_usage` - Storage used percentage
          🧹 `automation.frigate_media_cleanup` - Automatic cleanup automation

      - type: heading
        heading: 📹 Cameras

      - type: markdown
        content: |
          **Expected Camera Entities:**
          🚗 `camera.driveway` - Driveway camera feed
          🏠 `camera.garage` - Garage camera feed
          🌳 `camera.patio` - Patio camera feed
          🚪 `camera.front_door` - Front door camera feed
          🏡 `camera.backyard` - Backyard camera feed

  #################################################################
  # MEDIA - Detailed package breakdown
  #################################################################
  - path: media
    title: Media
    icon: mdi:play-circle-outline
    theme: default
    badges: []
    cards:
      - type: heading
        heading: 🎵 Media Package Structure

      - type: markdown
        content: |
          **Package Overview:**
          Audio players and entertainment system integrations including smart speakers and media hubs.

      - type: markdown
        content: |
          **2 Files in packages/media/:**
          🔊 google.yaml - Google Home/Nest speakers
          🎮 harmony_hub.yaml - Harmony Hub remote control

      - type: heading
        heading: 🔊 Google Home (google.yaml)

      - type: markdown
        content: |
          Voice assistants for TTS announcements and voice control. Connected smart speakers throughout home.

      - type: markdown
        content: |
          **Expected Entities:**
          🔊 `media_player.google_home` - Living room speaker
          📺 `media_player.google_nest_hub` - Kitchen display

      - type: heading
        heading: 🎮 Harmony Hub (harmony_hub.yaml)

      - type: markdown
        content: |
          IR remote control hub for managing TV, receivers, and entertainment devices. Includes activity macros for automating multi-device sequences.

      - type: markdown
        content: |
          **Expected Entities:**
          🎬 `media_player.harmony_hub` - Harmony Hub controller
          📺 `script.harmony_tv` - TV startup automation
          🎛️ `remote.harmony_hub` - Remote control interface

  #################################################################
  # SYSTEM - Detailed package breakdown
  #################################################################
  - path: system
    title: System Monitoring
    icon: mdi:cog
    theme: default
    badges: []
    cards:
      - type: heading
        heading: 📊 System Info (system_info.yaml)

      - type: markdown
        content: "System monitoring and information sensors"

      - type: markdown
        content: |
          **Expected Entities:**
          ⏱️ `sensor.system_uptime` - System uptime duration
          📊 `sensor.system_load` - CPU load average
          💾 `sensor.disk_free` - Available disk space

      - type: heading
        heading: ⚠️ Sensor Monitoring (sensor_reading_missing.yaml)

      - type: markdown
        content: |
          **Expected Input Booleans (Sensor Alarms):**
          🧊 `input_boolean.frys_reading_missing_alarm` - Freezer sensor alarm
          ❄️ `input_boolean.kyl_reading_missing_alarm` - Fridge sensor alarm
          🚿 `input_boolean.badrum_reading_missing_alarm` - Bathroom sensor alarm
          🚗 `input_boolean.garage_reading_missing_alarm` - Garage sensor alarm
          🏠 `input_boolean.friggebod_reading_missing_alarm` - Cold storage sensor alarm

  #################################################################
  # SENSORS - Detailed template sensor breakdown
  #################################################################
  - path: sensors
    title: Sensors & Templates
    icon: mdi:gauge
    theme: default
    badges: []
    cards:
      - type: heading
        heading: 🌡️ Sensor Package Structure

      - type: markdown
        content: |
          **Package Overview:**
          Custom template sensors for presence detection, environmental monitoring, and device status tracking across the home.

      - type: markdown
        content: "**Template Sensors - Part 1:**"

      - type: markdown
        content: |
          📍 **aqara_fp2_kitchen** - Aqara presence sensor in kitchen
          🎮 **harmony_hub** - Harmony Hub status tracking
          💡 **ljusstyrka** - Light brightness level calculations
          🌡️ **lumi_ht_agl02_hall** - Temperature/humidity hall sensor
          🏠 **lumi_ht_agl02_livingroom** - Temp/humidity living room
          🚿 **lumi_weather_bathroom** - Bathroom conditions
          🏡 **lumi_weather_uterum** - Outdoor room conditions
          ⚠️ **sensor_reading_missing** - Alerts for missing sensor data
          💻 **system_info** - Home Assistant system metrics
          🌡️ **timmerflotte_temp** - HVAC system temperature
          🌬️ **ventilation_system** - Ventilation status/control
          📱 **xiaomi_lumi_mgl01_hall** - Hall motion detection
          📱 **xiaomi_lumi_mgl01_livingroom** - Living room motion
          📱 **xiaomi_lumi_mgl01_louise_room** - Louise room motion

      - type: heading
        heading: Temperature & Humidity Sensors

      - type: markdown
        content: |
          **Expected Sensor Entities:**
          🌡️ `sensor.ute` - Outdoor temperature
          🏠 `sensor.inne_medel` - Indoor average temp
          🚿 `sensor.badrum_temperatur` - Bathroom temperature
          💧 `sensor.badrum_luftfuktighet` - Bathroom humidity
          🪟 `sensor.fukt_badrumsfonster` - Bathroom window humidity
          🏡 `sensor.uterum_temperatur` - Outdoor room temp
          🏠 `sensor.friggebod_temperatur` - Cold storage temp
          💧 `sensor.friggebod_luftfuktighet` - Cold storage humidity
          🚗 `sensor.garage_temperature` - Garage temp
          💧 `sensor.forrad_luftfuktighet` - Garage humidity
          📦 `sensor.forrad_temperature` - Storage room temp

      - type: heading
        heading: 🌡️ Aqara FP2 Kitchen (template_aqara_fp2_kitchen.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          👁️ `sensor.kitchen_presence` - Presence detection
          📍 `sensor.kitchen_distance` - Distance measurement

      - type: heading
        heading: 🎵 Harmony Hub Sensors (template_harmony_hub.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          🎥 `sensor.harmony_activity` - Current activity state

      - type: heading
        heading: 💡 Light Brightness (template_ljusstyrka.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          ☀️ `sensor.ljusstyrka_current` - Current light level
          🎯 `sensor.ljusstyrka_threshold` - Brightness threshold

      - type: heading
        heading: 🌡️ Lumi Humidity Sensors

      - type: markdown
        content: |
          **Expected Entities:**
          💪 `sensor.hall_is_temperature` - Hall temperature
          💧 `sensor.hall_is_humidity` - Hall humidity
          🌡️ `sensor.livingroom_temperature` - Living room temperature
          💧 `sensor.livingroom_humidity` - Living room humidity
          🌡️ `sensor.kitchen_temperature` - Kitchen temperature
          💧 `sensor.kitchen_humidity` - Kitchen humidity

      - type: heading
        heading: ⚠️ Warning Sensors (template_sensor_reading_missing.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          🚨 `binary_sensor.sensor_reading_missing` - Missing reading alerts

      - type: heading
        heading: 📊 System Info Templates (template_system_info.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          📊 `sensor.system_load` - CPU/system load
          ⏱️ `sensor.system_uptime` - System uptime

      - type: heading
        heading: 🌬️ Ventilation Sensors (template_ventilation_system.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          🟢 `sensor.ventilation_status` - System status
          📊 `sensor.ventilation_efficiency` - Efficiency percentage
          ⏱️ `sensor.ventilation_runtime` - Runtime hours

      - type: heading
        heading: 🏠 Building Occupancy (template_xiaomi_lumi_mgl01.yaml files)

      - type: markdown
        content: |
          **Expected Entities:**
          💪 `binary_sensor.hall_occupancy` - Hall occupancy status
          🛋️ `binary_sensor.livingroom_occupancy` - Living room occupancy

  #################################################################
  # CLIMATE & VENTILATION - Detailed breakdown
  #################################################################
  - path: climate
    title: Climate Control
    icon: mdi:thermometer
    theme: default
    badges: []
    cards:
      - type: heading
        heading: 🌡️ Climate Package Structure

      - type: markdown
        content: |
          **Package Overview:**
          HVAC thermostat control and temperature management for multiple zones including storage areas, garage, and ventilation system.

      - type: markdown
        content: |
          **4 Files in packages/climate/:**
          📦 forrad.yaml - Storage room climate control
          🏠 friggebod.yaml - Cold storage building
          🚗 garage.yaml - Garage temperature management
          🌬️ ventilation.yaml - Ventilation system control

      - type: heading
        heading: 🌡️ Förråd Climate (climate/forrad.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          🧱 `climate.forrad` - Thermostat control
          🌡️ `sensor.forrad_temperature` - Current temperature

      - type: heading
        heading: 🌡️ Friggebod Climate (climate/friggebod.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          🧱 `climate.friggebod` - Thermostat control
          🌡️ `sensor.friggebod_temperatur` - Current temperature

      - type: heading
        heading: 🌡️ Garage Climate (climate/garage.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          🧱 `climate.garage` - Thermostat control
          🌡️ `sensor.garage_temperature` - Current temperature

      - type: heading
        heading: 🌬️ Ventilation System (ventilation/ventilation_system.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          🔌 `switch.ventilationsaggregat` - Ventilation unit switch
          ⏱️ `sensor.ventilation_runtime` - System runtime
          🚨 `input_boolean.alarm_ventilation` - System alarm

      - type: heading
        heading: 🌬️ Ventilation Tuning (ventilation/ventilation_tuning.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          🎚️ `input_number.ventilation_speed` - Fan speed setting (0-100%)
          🎛️ `switch.shelly1_c45bbe77099d` - Forced fan override

      - type: heading
        heading: 🌬️ Ventilation Fan (ventilation/fan.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          💨 `fan.ventilation` - Fan control entity

  #################################################################
  # LIGHTS - Detailed breakdown per file
  #################################################################
  - path: lights
    title: Lighting
    icon: mdi:lightbulb
    theme: default
    badges: []
    cards:
      - type: heading
        heading: 💡 Lights Package Structure

      - type: markdown
        content: |
          **Package Overview:**
          Smart lighting system with zone-based control, scene management, and multiple lighting modes for different activities and times of day.

      - type: markdown
        content: |
          **14 Files in packages/lights/:**
          🎮 light_entities.yaml - Light control scripts
          🛏️ hall.yaml - Hall lighting zone
          🚿 bathroom.yaml - Bathroom lighting
          🍽️ kitchen.yaml - Kitchen lighting

      - type: heading
        heading: 🎮 Light Control (lights/light_entities.yaml)

      - type: markdown
        content: |
          **Expected Light Control Scripts:**
          ♻️ `script.lights_set_correct_modes` - Restore proper light modes
          ☀️ `script.button_daylight` - All lights on (daylight)
          🧹 `script.cleaning_lights_on` - Cleaning mode lights
          🌙 `script.button_goodnight` - Goodnight routine

      - type: heading
        heading: 🛏️ Hall (lights/hall.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          💡 `light.hall` - Hall main light
          🎯 `input_select.light_hall` - Hall light mode selector

      - type: heading
        heading: 🚿 Bathroom (lights/bathroom.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          💫 `light.bathroom` - Bathroom main light
          🎯 `input_select.light_bathroom` - Bathroom light mode selector

      - type: heading
        heading: 🍽️ Kitchen (lights/kitchen.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          💡 `light.kitchen` - Kitchen main light
          🎯 `input_select.light_kitchen` - Kitchen light mode selector

      - type: heading
        heading: 🏠 Inside (lights/inside.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          💡 `light.inside` - Inside main light
          🎯 `input_select.light_inside` - Inside light mode selector

      - type: heading
        heading: 🛁 Spa Bath (lights/spabad.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          💡 `light.spabad` - Spa bath main light

      - type: heading
        heading: 🏡 Facade (lights/facade.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          💫 `light.facade` - Facade exterior light
          🎯 `input_select.light_facade` - Facade light mode selector

      - type: heading
        heading: 🌳 Garden & Outdoor (lights/garden.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          💡 `light.garden` - Garden light
          🌴 `light.patio` - Patio/outdoor light

      - type: heading
        heading: 🎬 Scenes (lights/scenes.yaml)

      - type: markdown
        content: |
          **Expected Scene Entities:**
          🎆 `scene.evening` - Evening lighting mood
          🌤️ `scene.nighttime` - Nighttime lighting
          ☀️ `scene.daylight` - Daylight scene

      - type: heading
        heading: 🔄 Seasonal Groups (lights/light_groups_seasonal.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          💫 `light.seasonal_group` - Seasonal-based light groups

      - type: heading
        heading: 📅 Standard Groups (lights/light_groups_standard.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          💫 `light.standard_group` - Standard light groupings

      - type: heading
        heading: 📋 Light Package Files - Part 1

      - type: markdown
        content: |
          - bathroom.yaml
          - debug.yaml
          - facade.yaml
          - garden.yaml
          - hall.yaml
          - inside.yaml
          - kitchen.yaml

      - type: heading
        heading: 📋 Light Package Files - Part 2

      - type: markdown
        content: |
          - light_entities.yaml
          - light_groups_seasonal.yaml
          - light_groups_standard.yaml
          - outdoor_room.yaml
          - restore_all.yaml
          - scenes.yaml
          - spabad.yaml

  #################################################################
  # INTEGRATIONS - Detailed breakdown
  #################################################################
  - path: integrations
    title: Integrations
    icon: mdi:puzzle
    theme: default
    badges: []
    cards:
      - type: heading
        heading: 🔌 Integration Package Structure

      - type: markdown
        content: |
          **Package Overview:**
          Third-party service integrations including MQTT messaging, vehicle tracking, fitness platforms, and smart home protocols.

      - type: markdown
        content: |
          **6 Files in packages/integrations/:**
          📡 mqtt.yaml - MQTT message broker
          🚗 volvo.yaml - Volvo car integration
          🚴 zwift.yaml - Zwift fitness tracking
          📻 tellstick.yaml - Tellstick RF devices
          🔊 tts.yaml - Text-to-speech service
          🎮 media_player.yaml - Media player control

      - type: markdown
        content: "**Integration Details:**"

      - type: markdown
        content: |
          📡 **MQTT** - Real-time messaging protocol for device communication
           **Volvo** - Vehicle location, status, and battery monitoring
          🚴 **Zwift** - Cycling fitness platform integration
          📻 **Tellstick** - RF protocol for wireless devices
          🔊 **TTS** - Google Home text-to-speech announcements
          🎮 **Media Player** - Unified media player controls

      - type: heading
        heading: 🔌 MQTT (integrations/mqtt.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          🌐 `binary_sensor.mqtt_connection` - Broker connection status
          📨 `sensor.mqtt_stats` - Message statistics

      - type: heading
        heading:  Volvo (integrations/volvo.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          📍 `device_tracker.volvo` - Vehicle location
          🔴 `binary_sensor.volvo_online` - Online status

      - type: heading
        heading: 🚴 Zwift (integrations/zwift.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          🏋️ `sensor.zwift_status` - Fitness activity status
          🔴 `binary_sensor.zwift_online` - Online status

      - type: heading
        heading: 📻 Tellstick (integrations/tellstick.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          🔄 `binary_sensor.tellstick_connection` - Connection status

      - type: heading
        heading: 🔊 Text-to-Speech (integrations/tts.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          🎤 `media_player.tts` - TTS media player

      - type: heading
        heading: 🎮 Media Player (integrations/media_player.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          🎬 `media_player.harmony_hub` - Harmony Hub player
          🔊 `media_player.google_home` - Google Home speaker

  #################################################################
  # PRESENCE - Detailed breakdown
  #################################################################
  - path: presence
    title: Presence Tracking
    icon: mdi:home-account
    theme: default
    badges: []
    cards:
      - type: heading
        heading: 👥 Presence Package Structure

      - type: markdown
        content: |
          **Package Overview:**
          Family member location tracking, home/away status, and presence-based automations and notifications.

      - type: markdown
        content: |
          **3 Files in packages/presence/:**
          👥 person.yaml - Family members tracking
          🏠 home_away.yaml - Home status control
          💼 working_at_home.yaml - Work status indicator

      - type: heading
        heading: 👥 People (presence/person.yaml)

      - type: markdown
        content: |
          **Expected Person Entities:**
          👨 `person.micke` - Micke location
          👩 `person.annelie` - Annelie location
          👧 `person.louise` - Louise location
          👨 `person.andreas` - Andreas location
          👴 `person.herbert` - Herbert location
          👵 `person.margareta` - Margareta location
          👦 `person.erik` - Erik location

      - type: heading
        heading: 🏠 Home Status (presence/home_away.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          🏡 `input_select.home_status` - Home status mode selector
          👥 `binary_sensor.someone_home` - Anyone home indicator

      - type: heading
        heading: 💼 Working from Home (presence/working_at_home.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          💻 `binary_sensor.working_at_home` - Work from home status

      - type: markdown
        content: |
          **Files in packages/presence/**:
          - home_away.yaml
          - person.yaml
          - working_at_home.yaml

  #################################################################
  # HELPERS - Detailed breakdown
  #################################################################
  - path: helpers
    title: Helpers & Input Controls
    icon: mdi:tune
    theme: default
    badges: []
    cards:
      - type: heading
        heading: ✓ Helpers Package Structure

      - type: markdown
        content: |
          **Package Overview:**
          Input helpers for user controls including toggles, dropdowns, and configuration selectors used throughout automations.

      - type: markdown
        content: |
          **2 Files in packages/helpers/:**
          ✓ input_booleans.yaml - Toggle switches & alarms
          🎯 input_selects.yaml - Mode selection dropdowns

      - type: heading
        heading: ✓ Input Booleans (helpers/input_booleans.yaml)

      - type: markdown
        content: |
          **Expected Input Boolean Entities:**
          🌬️ `input_boolean.alarm_ventilation` - Ventilation system alarm
          🏠 `input_boolean.guest_mode` - Guest mode toggle
          ⚙️ `input_boolean.automation_enabled` - Automations on/off

      - type: heading
        heading: 🎯 Input Selects (helpers/input_selects.yaml)

      - type: markdown
        content: |
          **Expected Input Select Entities:**
          🏡 `input_select.home_status` - Home status mode
          💡 `input_select.light_inside` - Inside lighting mode
          🍽️ `input_select.light_kitchen` - Kitchen lighting mode
          🛏️ `input_select.light_hall` - Hall lighting mode
          🚿 `input_select.light_bathroom` - Bathroom lighting mode
          🍂 `input_select.season` - Current season setting

  #################################################################
  # AUTOMATIONS - Detailed breakdown
  #################################################################
  - path: automations
    title: Automations
    icon: mdi:robot
    theme: default
    badges: []
    cards:
      - type: heading
        heading: 🤖 Automations Package Structure

      - type: markdown
        content: |
          **Package Overview:**
          Home automation routines including lighting control, presence-based triggers, and scheduled tasks.

      - type: markdown
        content: |
          **1 File in packages/automations/:**
          💡 input_select_light_control.yaml - Lighting mode automation

      - type: heading
        heading: 💡 Light Control (automations/input_select_light_control.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          ⚙️ `automation.input_select_light_control` - Light mode automation
          ♻️ `script.lights_set_correct_modes` - Light sync script

      - type: markdown
        content: |
          **Files in packages/automations/**:
          - input_select_light_control.yaml

  #################################################################
  # SCRIPTS - Detailed breakdown
  #################################################################
  - path: scripts
    title: Scripts
    icon: mdi:script-text-outline
    theme: default
    badges: []
    cards:
      - type: heading
        heading: 📜 Scripts Package Structure

      - type: markdown
        content: |
          **Package Overview:**
          Reusable automation scripts for common routines like goodnight sequence and light control.

      - type: markdown
        content: |
          **1 File in packages/scripts/:**
          🌙 godnatt.yaml - Bedtime shutdown routine

      - type: heading
        heading: 🌙 Goodnight Routine (scripts/godnatt.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          😴 `script.godnatt` - Full goodnight routine
          🌙 `script.button_goodnight` - Quick goodnight trigger

      - type: markdown
        content: |
          **Files in packages/scripts/**:
          - godnatt.yaml

  #################################################################
  # ALERTS - Detailed breakdown
  #################################################################
  - path: alerts
    title: Alerts & Notifications
    icon: mdi:bell-alert
    theme: default
    badges: []
    cards:
      - type: heading
        heading: ⚠️ Alerts Package Structure

      - type: markdown
        content: |
          **Package Overview:**
          Environmental and device monitoring alerts for temperature extremes, offline devices, and system status issues.

      - type: markdown
        content: |
          **2 Files in packages/alerts/:**
          🌡️ temperature.yaml - Extreme temperature warnings
          🔌 devices_offline.yaml - Device connectivity alerts

      - type: heading
        heading: 🌡️ Temperature (alerts/temperature.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          ⚠️ `binary_sensor.temperature_alert` - Alert status indicator
          🔥 `automation.temperature_too_high` - High temperature automation

      - type: heading
        heading: 🔌 Device Status (alerts/devices_offline.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          📴 `binary_sensor.device_offline` - Device status indicator
          ⚠️ `automation.device_offline_alert` - Offline alert automation

      - type: markdown
        content: |
          **Files in packages/alerts/**:
          - devices_offline.yaml
          - temperature.yaml

  #################################################################
  # NOTIFICATIONS - Detailed breakdown
  #################################################################
  - path: notifications
    title: Notifications
    icon: mdi:bell
    theme: default
    badges: []
    cards:
      - type: heading
        heading: � Notifications Package Structure

      - type: markdown
        content: |
          **Package Overview:**
          Event-based notifications for user alerts including doorbell, security events, and system notifications.

      - type: markdown
        content: |
          **1 File in packages/notifications/:**
          🚪 doorbell.yaml - Doorbell event notifications

      - type: heading
        heading: �🚪 Doorbell (notifications/doorbell.yaml)

      - type: markdown
        content: |
          **Expected Entities:**
          🔔 `automation.doorbell_notification` - Doorbell alert automation
          🚪 `binary_sensor.doorbell_pressed` - Button press status

      - type: markdown
        content: |
          **Files in packages/notifications/**:
          - doorbell.yaml
